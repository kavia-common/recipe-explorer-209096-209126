This folder holds public assets like default Open Graph/Twitter images.
Place a 1200x630 image at:
- /public/assets/og-default.png

If not provided, dynamic routes will still render image cards via next/og.
